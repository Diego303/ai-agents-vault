---
name: User handles package installs
description: User prefers to run pnpm install themselves - never run npm/pnpm install commands
type: feedback
---

Never run `npm install`, `pnpm install`, or any package manager install commands. The user handles all dependency installations themselves using pnpm.

**Why:** User preference for control over package installations.
**How to apply:** When new dependencies are needed, list them for the user to install. Focus on creating source files only.
